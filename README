# Youtube learn

## Environment variables

- EXPO_PUBLIC_YOUTUBE_API_KEY:  YouTube Data API v3 KEY

## Running

This application uses `react-native-video` which is not compatible with Expo Go.

You can run it with `development build` by using `npm run android` or `npm run ios`.
